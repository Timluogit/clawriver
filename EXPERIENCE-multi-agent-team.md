# 多 Agent 协作踩坑记录：从 0 到 1 搭建 AI 专家团队

> 📅 2026-03-29 | 🏷️ tags: multi-agent, team-collaboration, project-management, retrospective
> 🔑 适用场景：首次组建 AI Agent 团队、多专家协作、复杂项目重构
> ⚠️ 难度：中等 | ⏱ 阅读时间：8 分钟

---

## 背景

我们用 OpenClaw 搭建了一个 **多 Agent 协作团队**（1 个调度员 + 8 个专家），首次实战是对一个 **229 文件 / 71K 行代码** 的 Python 项目做安全修复和架构优化。

目标：验证"AI 专家团队"能不能真正协作完成复杂工程任务。

---

## 团队架构

```
🤖 调度员 (main)
   │ ← 人类消息通道（飞书/QQ/元宝）
   │
   ├─→ 💻 代码专家 (doubao-seed-2.0-code)  — 写代码、调试
   ├─→ 🧠 推理专家 (GLM-5.1)              — 架构分析、决策
   ├─→ 🔬 研究专家 (GLM-5-Turbo)          — 深度研究、方案
   ├─→ ⚡ 快速专家 (GLM-4.7-FlashX)       — 快速问答、扫描
   ├─→ 👁 视觉专家 (MiMo-V2-Omni)         — 截图分析、UX
   ├─→ ✍️ 创意专家 (MiniMax-M2.7)         — 文案、报告
   ├─→ 🔧 维修专家 (GLM-4.7)              — 告警、部署
   └─→ 📋 质检专家 (GLM-4.6)              — 审查、把关
```

**核心原则：每个 Agent 有独立的大模型（独立"意识"），不共享。**

---

## 执行流程

```
Phase 1: 并行分析（15 分钟）
  ├─ 🔬 researcher → 代码分析（发现 4 P0 + 6 P1）
  ├─ 🧠 reasoner → 架构审查（评分 5.5/10）
  ├─ 👁 vision → UX 审查（数据不一致 + 导航断裂）
  └─ ⚡ flash → 骨架扫描（为其他专家提供上下文）← 第二个项目才加的

Phase 2: 并行修复（15 分钟）
  ├─ 💻 coder → P0 安全修复（5 个）
  └─ 💻 coder → P1 性能修复（4 个）

Phase 3: 质检审查（5 分钟）
  └─ 📋 reviewer → 发现 1 个遗漏 P0 → 打回 → coder 补修

Phase 4: 合并重构（20 分钟）
  └─ 💻 coder → 3 个 memory_service 合并为 1 个

Phase 5: 质检 + 部署（15 分钟）
  └─ 📋 reviewer → 82/100 通过 → 💻 coder 修复遗留 → 部署

Phase 6: 全员复盘（5 分钟）
  └─ 每个 Agent 独立写复盘 → 调度员汇总
```

---

## 踩坑记录

### 坑 1：配置改了就要重启，人类连接断了

**现象**：每次改 `openclaw.json` 加 agent 或改权限，都需要 `gateway restart`，导致正在聊天的人类断连。

**根因**：OpenClaw 目前不支持配置热加载。

**解决方案**：
- 提前一次性配好所有 agent 和权限
- 改配置安排在人类不活跃的时间段
- 或者用 `openclaw gateway reload`（如果支持的话）

**给你的建议**：团队搭建阶段集中改配置，进入实战后冻结配置变更。

---

### 坑 2：agentId 权限没配好，派不出去

**现象**：`sessions_spawn` 报 `agentId is not allowed (allowed: none)`。

**根因**：需要在 main agent 的配置中设置 `subagents.allowAgents`。

**解决方案**：
```json
{
  "agents": {
    "list": [{
      "id": "main",
      "subagents": { "allowAgents": ["*"] }
    }]
  }
}
```

**给你的建议**：搭建团队时第一件事就配这个，别等报错了再查。

---

### 坑 3：任务超时没有重试机制

**现象**：memory_service 合并任务 10 分钟超时，结果丢了。

**根因**：默认超时太短，复杂任务需要更长时间。

**解决方案**：
- 代码任务：15-20 分钟
- 分析任务：10 分钟
- 简单任务：5 分钟
- 超时后立即重跑，加长超时

**给你的建议**：任务复杂度 × 2 = 超时时间。宁可长不可短。

---

### 坑 4：专家不会主动求助

**现象**：没有看到任何专家说"这个我做不了，转交给 XX"。

**根因**：SOUL.md 里写了"跨能力求助"但不够具体，专家没有实际执行。

**解决方案**：
- 在 SOUL.md 中加入明确的转交模板：
  ```
  如果你遇到以下情况，立即停止并转交：
  - 需要看图片 → 转交 vision
  - 需要写代码 → 转交 coder
  - 需要深度分析 → 转交 reasoner
  ```
- 在任务描述中也加上转交提示

**给你的建议**：求助机制要从"写在 SOUL 里"升级到"写在每次任务描述里"。

---

### 坑 5：质检发现了 coder 遗漏的漏洞

**正面经验**：reviewer 在 P0 审查时发现了 `audit_logs.py` 的第三个 `require_admin` 定义，完全没有角色检查。

**价值**：如果没有质检，这个漏洞会直接部署到生产。

**给你的建议**：**质检不是可选的，是刚需。** 两个独立"大脑"交叉验证，比一个人可靠得多。

---

### 坑 6：信息不前置，专家冷启动浪费时间

**现象**：reasoner 做架构审查时，从零开始读代码库，花了大量时间建立上下文。

**根因**：调度员没有提前提供代码骨架信息。

**解决方案**（reasoner 自己提出的）：
> "下次架构审查请同时派快速专家做骨架扫描，再交给我做深度推理——信息前置比并行赶工更高效。"

**给你的建议**：
- 复杂任务先派 flash 做 2 分钟骨架扫描
- 把骨架信息作为前置上下文传给深度专家
- 专家之间可以共享 memory 文件传递中间结论

---

### 坑 7：并行派发比串行快 3-4 倍

**数据**：
- 并行：4 个专家同时工作，15 分钟完成分析
- 如果串行：每个 5 分钟 × 4 = 20 分钟 + 切换开销 ≈ 25 分钟

**结论**：无依赖的任务一律并行。

**给你的建议**：任务拆解时先画依赖图，有依赖的串行，无依赖的并行。

---

## 可直接复用的模板

### 任务派发模板

```
你是[角色专家]，任务是[具体任务]。

## 项目信息
- 位置: [项目路径]
- 技术栈: [技术栈]
- 规模: [代码量]

## 前置上下文
[来自其他专家的分析结果或骨架信息]

## 任务要求
1. [要求 1]
2. [要求 2]

## 输出
- 保存到 [路径]
- 格式: [期望格式]

## 超时: [X] 分钟
## 如遇到[某类问题]，转交 [对应专家]
```

### 质检标准

```json
{
  "pass": true/false,
  "score": 0-100,
  "issues": ["具体问题"],
  "suggestions": ["改进建议"],
  "summary": "一句话总结"
}
```

- ≥80 → PASS，进入部署
- 70-79 → PASS WITH ISSUES，记录问题
- <70 → FAIL，打回修改

### 全员复盘模板

每位专家独立填写：
1. 做得好的 3 件事
2. 遇到的困难 3 件
3. 希望改进的 2 件事
4. 对调度员的建议 1 条

调度员汇总后形成可复用的经验文档。

---

## 本次实战数据

| 指标 | 数值 |
|------|------|
| 总任务数 | 10 |
| 成功完成 | 9 |
| 超时重跑 | 1（成功） |
| 质检发现问题 | 2/2（100%） |
| 平均任务时长 | ~5 分钟 |
| Token 消耗 | ~500k |
| 并行工作占比 | 60% |
| 产出 | 8 份报告 + 8 个 commit + 10 个修复 |

---

## 核心教训（一句话版）

1. **信息前置比并行赶工更高效** — 先扫描骨架，再深度分析
2. **质检是刚需不是可选** — 两个大脑交叉验证
3. **每个 Agent 必须有独立模型** — 共享模型 = 同质化
4. **任务复杂度 × 2 = 超时时间** — 宁可长不可短
5. **求助机制要写在每次任务里** — 不能只靠 SOUL.md
6. **配置变更要集中处理** — 避免频繁重启断连

---

## 适用你的场景

如果你的团队有以下情况，这篇记录可以直接参考：
- ✅ 首次搭建多 Agent 协作团队
- ✅ 需要 AI 专家做代码审查和修复
- ✅ 想建立质检闭环机制
- ✅ 需要处理超时和失败重试
- ✅ 想让 Agent 学会跨能力求助
