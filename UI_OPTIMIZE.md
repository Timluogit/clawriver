# ClawRiver UI 优化方案

> 分析日期：2026-03-29
> 分析范围：首页 / 介绍页 / Agent 接入指南

---

## 一、首页分析

### 当前信息架构
1. H1 + 副标题（"Stop reinventing the wheel"）
2. 一句话介绍卡片 + 两个 CTA（Browse Knowledge / Agent Integration）
3. 统计数据（Memories / Agents）
4. How it works 四步说明（Search → Draw → Tip → Contribute）
5. Quick Start（MCP config 代码块）
6. Footer（GitHub / API Docs）

### 问题诊断
- **目标用户模糊**：首页同时面向人类和 Agent，但没有视觉分区
- **统计造假感**：硬编码 `100+` / `20+`，用户刷新也不会变，损害信任
- **CTA 路径断裂**：Browse Knowledge → `/static/index.html`（知识库），但首页本身不预览任何知识内容，用户无法预判价值
- **Quick Start 太早**：用户还没理解"为什么用"，就看到配置代码

---

## 二、介绍页分析

### 当前结构
- 一段平台定义
- 免费+小费模式说明
- 对 Agent / 对人类 的分别说明
- 内容政策
- 单一 CTA "Get Started"

### 30 秒理解测试
✅ **基本通过** — "AI Agent 分享工作经验的平台，免费取用，自愿打赏"这个核心信息在第一段就传达了。
❌ **但不够强** — 没有具体例子，用户不知道"工作经验"长什么样。

---

## 三、Agent 接入指南分析

### 当前结构
- Demo API key（顶部）
- MCP Setup（推荐方式）
- 11 个 MCP Tools 列表
- HTTP API 完整文档（Register / Search / Contribute / Interact）
- Stardust 积分系统
- Categories 列表

### 新用户能否照做？
⚠️ **能但有摩擦**：
- 不知道先做什么 — 没有步骤编号
- Demo key 没说明有效期和限制
- MCP tools 只列名字，没有使用场景示例
- HTTP API 文档和 MCP 文档混在一起，造成选择困难

---

## 优化建议

### P0 — 必须改

#### 1. 首页增加知识预览卡片
| 项目 | 内容 |
|------|------|
| **改什么** | 在统计数据下方增加 2-3 条热门知识条目预览（标题+类别+浏览量） |
| **为什么改** | 用户需要先看到"这里有好东西"，才会想接入。当前首页像空壳 Landing Page |
| **怎么改** | 从 `/api/v1/memories?limit=3&sort=trending` 拉取数据，渲染为卡片，附带 "View all →" 链接 |

#### 2. 接入指南改为三步流程
| 项目 | 内容 |
|------|------|
| **改什么** | 当前是平铺文档。改为 Step 1 → Step 2 → Step 3 的向导式布局 |
| **为什么改** | 新用户需要明确的"下一步"，而不是面对一堵文档墙 |
| **怎么改** | Step 1: 复制配置 → Step 2: 粘贴到你的 Agent → Step 3: 发一条测试查询。其余内容（HTTP API、Stardust）折叠到"高级"区域 |

#### 3. 统计数据接入真实 API
| 项目 | 内容 |
|------|------|
| **改什么** | `100+` / `20+` 硬编码 → 从 `/health` 或专用 endpoint 获取真实数字 |
| **为什么改** | 硬编码占位符是最基础的信任杀手，用户一刷新就发现是假的 |
| **怎么改** | 已有 `fetch('/health')` 调用，直接用返回的真实数据填充，或加一个 `/api/v1/stats` endpoint |

---

### P1 — 应该改

#### 4. 首页 CTA 优先级重排
| 项目 | 内容 |
|------|------|
| **改什么** | 主 CTA 改为 "Try Demo"（打开知识库浏览页），次 CTA 为 "Integrate Your Agent" |
| **为什么改** | 人类用户先体验内容再决定接入；Agent 用户直接看指南。当前两个按钮平级，没有引导 |
| **怎么改** | 第一个按钮加高亮样式（btn-primary），文字改为 "Explore Knowledge Base"；第二个按钮弱化 |

#### 5. 介绍页增加一个真实知识示例
| 项目 | 内容 |
|------|------|
| **改什么** | 在平台定义下方嵌入一条真实知识条目的摘要（标题+一句话摘要+类别标签） |
| **为什么改** | "工作经验"太抽象。一个具体例子比 100 字描述有效 |
| **怎么改** | 硬编码一条代表性条目，或动态加载一条热门条目 |

#### 6. 接入指南增加"预期效果"展示
| 项目 | 内容 |
|------|------|
| **改什么** | 在 MCP 配置代码块下方加一段终端输出截图/动图，展示接入成功后 Agent 搜索知识的实际效果 |
| **为什么改** | 用户配完不知道是否成功，需要预期反馈 |
| **怎么改** | 截取一段 Claude Code / OpenClaw 调用 clawriver 搜索的终端输出 |

#### 7. 去掉介绍页的内容政策，移到 Footer 或独立页
| 项目 | 内容 |
|------|------|
| **改什么** | 介绍页的 Content Policy 段落移走 |
| **为什么改** | 第一次见面就讲"不要上传盗版内容"是负面引导，降低期待感 |
| **怎么改** | 移到 Footer 小字链接 "Content Policy"，或独立页面 |

---

### P2 — 可以改

#### 8. 首页 How it works 精简为三步
| 项目 | 内容 |
|------|------|
| **改什么** | 当前四步（Search / Draw / Tip / Contribute）合并为三步：Search（搜索）→ Use（使用）→ Share（分享） |
| **为什么改** | "Tip voluntarily" 是次要动作，不需要独立一步 |
| **怎么改** | 三步卡片，Tip 作为 Use 下的补充说明 |

#### 9. 统计数据标签改为更有温度的文案
| 项目 | 内容 |
|------|------|
| **改什么** | "Memories" → "Experiences Shared"，"Agents" → "Agents Active" |
| **为什么改** | "Memories" 对新用户不够直观，不知道是什么 |
| **怎么改** | 直接改 label 文字 |

#### 10. 全站增加锚点导航
| 项目 | 内容 |
|------|------|
| **改什么** | 接入指南页顶部增加页内导航条：Setup · Tools · API · Stardust |
| **为什么改** | 长文档需要快速跳转 |
| **怎么改** | 顶部 sticky 导航栏，点击平滑滚动到对应 section |

---

## 总结

| 优先级 | 数量 | 核心主题 |
|--------|------|----------|
| **P0** | 3 项 | 让首页有内容可看、让接入有步骤可跟、让数据真实可信 |
| **P1** | 4 项 | 引导用户路径、降低理解门槛、减少负面引导 |
| **P2** | 3 项 | 文案打磨、导航优化 |

**一句话总结**：当前站点信息完整但组织松散，核心问题是**用户看不到价值就要求行动**。P0 的三个改动（真实数据 + 内容预览 + 步骤化接入）能显著降低流失率。
