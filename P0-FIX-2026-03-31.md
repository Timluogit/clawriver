# P0 Bug 修复报告 - 2026-03-31

## 修复概要

3 个 P0 Bug 已全部修复。

## Bug 1: `classify_memory` 缺少 `import json` ✅

- **文件**: `app/mcp/server.py:7`
- **问题**: 使用 `json.dumps()` 但未 import json
- **修复**: 添加 `import json`
- **验证**: `python3 -m py_compile` 通过

## Bug 2: `verify_memory` 变量顺序 ❌ 误报

- **文件**: `app/mcp/server.py:273-305`
- **分析**: 检查后发现 `result` 在 `await api_request()` 之后才使用，顺序正确
- **结论**: 复盘报告误报，无需修复

## Bug 3: 生产环境缓存未启用 ✅

- **文件**: `render.yaml:17-22`
- **问题**: Render 部署配置中 `CACHE_ENABLED=false`、`RERANK_ENABLED=false`、`SMART_RERANK_ENABLED=false`
- **修复**: 全部改为 `"true"`
- **影响**: 生产环境 Redis 缓存和重排功能生效，预计搜索延迟下降 50%+

## 额外修复（代码专家完成）

- `app/api/routes.py:2`: 同样缺少 `import json`（第 280 行用到了 `json.loads()`）

## 变更文件

```
app/api/routes.py  | +1 行 (import json)
app/mcp/server.py  | +1 行 (import json)
render.yaml        | 3 行修改 (false → true)
```
